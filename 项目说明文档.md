# RAG 智能问答系统 — 项目说明文档

## 1. 项目概述

### 1.1 做了什么

这是一个 **RAG（Retrieval-Augmented Generation，检索增强生成）** 系统。简单来说，它能做两件事：

1. **上传文档**：用户上传 `.txt` 或 `.pdf` 文件，系统会自动把文档内容切成小块，转换成数学向量（叫 embedding），存到数据库里。
2. **智能问答**：用户输入问题，系统会从数据库里找到最相关的文档片段，把这些片段和问题一起发给大语言模型（LLM），让 LLM 基于文档内容生成回答。

### 1.2 为什么需要 RAG

直接用大语言模型（如 ChatGPT）回答问题有一个缺点：它只能用自己训练时学到的知识，不知道你私有文档里的内容。RAG 的作用就是**先检索，再生成**——把你的文档内容喂给 LLM，让它基于这些内容回答，这样回答就准确且有据可依了。

### 1.3 核心价值

- 基于**私有文档**的问答，不需要把数据上传到第三方平台
- 检索 + 生成的两阶段架构，回答有来源可追溯
- 用户隔离，每个用户只能看到自己上传的文档

---

## 2. 技术架构

### 2.1 整体架构

```
用户浏览器
    │
    ▼
┌─────────────────────────────────────────────┐
│  FastAPI 服务器 (端口 8080)                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ 用户 API  │  │ 文档 API  │  │ 问答 API  │   │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘   │
│       │              │              │         │
│  ┌────▼─────────────▼──────────────▼────┐    │
│  │          Service 业务层               │    │
│  │  (文档处理 / Embedding / RAG 检索生成) │    │
│  └────┬──────────────┬──────────────────┘    │
│       │              │                       │
│  ┌────▼────┐   ┌─────▼──────┐               │
│  │PostgreSQL│   │ 小米 MiMo  │               │
│  │ (数据库)  │   │ (LLM API)  │               │
│  └─────────┘   └────────────┘               │
└─────────────────────────────────────────────┘
```

简单来说：
- **前端**（浏览器里的网页）发送请求给 **FastAPI 后端**
- 后端负责：存用户信息、处理文档、调用 Embedding 模型、调用 LLM 生成回答
- **PostgreSQL** 存储所有数据（用户、文档、文档向量）
- **小米 MiMo API** 提供大语言模型的文本生成能力
- **本地 text2vec 模型** 提供文本转向量的能力（不需要联网）

### 2.2 技术栈一览

| 层次 | 技术 | 作用 |
|------|------|------|
| Web 框架 | FastAPI | 处理 HTTP 请求，提供 REST API |
| 服务器 | Uvicorn | 运行 FastAPI 的 ASGI 服务器 |
| 数据库 | PostgreSQL 16 | 存储用户、文档、向量数据 |
| ORM | SQLAlchemy 2.0（异步） | 用 Python 类操作数据库，不用写 SQL |
| 数据库迁移 | Alembic | 管理数据库表结构的版本变更 |
| LLM 框架 | LangChain | 封装 LLM 调用、Prompt 模板、链式调用 |
| LLM 服务 | 小米 MiMo API | 大语言模型，负责生成回答 |
| Embedding | text2vec-base-chinese | 本地模型，把文本转成 768 维向量 |
| 文本切分 | LangChain TextSplitter | 把长文档切成 500 字的小块 |
| PDF 解析 | pypdf | 从 PDF 文件中提取文字 |
| 密码加密 | bcrypt | 对用户密码进行哈希加密存储 |
| 配置管理 | pydantic-settings | 从 .env 文件读取配置 |
| 前端 | 原生 HTML/CSS/JS | 单页面应用，无框架 |

---

## 3. 项目目录结构

```
rag-system/
├── .env                        # 环境变量配置文件（数据库连接、API Key 等）
├── alembic.ini                 # Alembic 数据库迁移工具的配置文件
├── requirements.txt            # Python 依赖包列表
├── start_server.py             # 服务器启动脚本
│
├── alembic/                    # 数据库迁移目录
│   ├── env.py                  # Alembic 环境配置（连接数据库的方式）
│   └── versions/
│       ├── 001_initial_schema.py    # 第 1 次迁移：创建 users/documents/document_chunks 三张表
│       └── 002_add_password_hash.py # 第 2 次迁移：给 users 表加 password_hash 字段
│
├── app/                        # 应用主目录（所有业务代码都在这里）
│   ├── main.py                 # 应用入口：创建 FastAPI 实例，注册路由和中间件
│   │
│   ├── core/                   # 核心配置层
│   │   ├── config.py           # 从 .env 读取配置项（数据库URL、API Key、模型名等）
│   │   ├── database.py         # 创建数据库引擎和会话工厂（连接池管理）
│   │   └── deps.py             # FastAPI 依赖注入（解析租户ID等公共逻辑）
│   │
│   ├── models/                 # 数据库模型层（SQLAlchemy ORM）
│   │   ├── base.py             # ORM 基类：所有表共用的时间戳和租户ID
│   │   ├── user.py             # users 表：用户信息 + 密码哈希
│   │   ├── document.py         # documents 表：文档元信息（标题、类型、文件名）
│   │   └── document_chunk.py   # document_chunks 表：文档切片 + 向量 embedding
│   │
│   ├── schemas/                # 请求/响应模型（Pydantic）
│   │   ├── chat.py             # 问答接口的请求体和响应体
│   │   └── document.py         # 文档接口的请求体和响应体
│   │
│   ├── api/                    # API 路由层
│   │   └── v1/
│   │       ├── router.py       # 路由聚合：把 users/documents/chat 三个路由器合并
│   │       ├── users.py        # 用户 API：注册、登录
│   │       ├── documents.py    # 文档 API：上传、列表
│   │       └── chat.py         # 问答 API：检索片段、流式问答
│   │
│   ├── services/               # 业务逻辑层（核心处理流程）
│   │   ├── document_service.py # 文档处理流水线：解析→切分→向量化→入库
│   │   ├── embedding_service.py# Embedding 服务：调用本地 text2vec 模型
│   │   └── rag_service.py      # RAG 服务：检索相关片段 → 拼接上下文 → 调用 LLM 生成
│   │
│   ├── utils/                  # 工具函数
│   │   ├── file_parser.py      # 文件解析：.txt 解码 / .pdf 提取文字
│   │   └── text_splitter.py    # 文本切分：按 500 字切块，前后重叠 50 字
│   │
│   └── static/                 # 前端静态文件
│       └── index.html          # 单页面应用（HTML + CSS + JavaScript 全在一个文件里）
│
└── scripts/                    # 脚本工具
    └── seed_user.py            # 种子数据脚本（用于初始化测试用户）
```

---

## 4. 核心流程详解

### 4.1 用户注册与登录流程

#### 流程图

```
注册：
  用户输入用户名+密码
      │
      ▼
  POST /api/v1/users/register   ←── app/api/v1/users.py
      │
      ▼
  检查用户名是否已存在
      │
      ├─ 已存在 → 返回 409 错误："用户名已存在"
      │
      └─ 不存在 → bcrypt 加密密码 → 写入 users 表 → 返回成功

登录：
  用户输入用户名+密码
      │
      ▼
  POST /api/v1/users/login      ←── app/api/v1/users.py
      │
      ▼
  从数据库查找用户 → bcrypt 验证密码
      │
      ├─ 不匹配 → 返回 401 错误："用户名或密码不正确"
      │
      └─ 匹配 → 返回 user_id + username
```

#### 代码详解

**`app/api/v1/users.py`** — 用户接口

```python
# 注册接口的核心逻辑（简化版）：

@router.post("/register")
async def register(request: RegisterRequest, db: AsyncSession = Depends(get_db)):
    # 1. 查数据库：这个用户名是否已经被注册了？
    result = await db.execute(
        select(User).where(User.username == request.username)
    )
    if result.scalar_one_or_none() is not None:
        raise HTTPException(status_code=409, detail="用户名已存在")

    # 2. 用 bcrypt 把密码加密（哈希），原始密码不会存到数据库
    hashed = bcrypt.hashpw(request.password.encode(), bcrypt.gensalt())

    # 3. 创建用户记录，写入数据库
    user = User(username=request.username, password_hash=hashed)
    db.add(user)
    await db.commit()  # 提交事务，数据真正写入 PostgreSQL

    return {"user_id": user.id, "username": user.username}
```

**为什么密码要加密？**
直接存明文密码，数据库被攻破后所有用户密码都会泄露。bcrypt 是一种**单向哈希**算法，加密后无法反推原始密码，登录时只需要把用户输入的密码也用 bcrypt 加密，然后对比两个哈希值是否一致。

---

### 4.2 文档上传与向量化流程（重点）

这是整个系统最核心的流程之一。当用户上传一个文档时，系统要经过 **4 个步骤** 才能把文档变成可检索的数据。

#### 流程图

```
用户上传文件（.txt / .pdf）
    │
    ▼
┌──────────────────────────────────────────────────┐
│ 步骤 1：文件解析（app/utils/file_parser.py）       │
│   .txt → 用 UTF-8/GBK 解码为纯文本                │
│   .pdf → 用 pypdf 逐页提取文字                     │
│   输出：一整段纯文本字符串                          │
└──────────────┬───────────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────────┐
│ 步骤 2：文本切分（app/utils/text_splitter.py）     │
│   用 LangChain RecursiveCharacterTextSplitter     │
│   按 500 字切块，前后重叠 50 字                     │
│   输出：多个文本片段（chunk）列表                   │
└──────────────┬───────────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────────┐
│ 步骤 3：向量化（app/services/embedding_service.py）│
│   用本地 text2vec-base-chinese 模型               │
│   每个 chunk → 一个 768 维的浮点数向量              │
│   输出：每个 chunk 对应一个向量                     │
└──────────────┬───────────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────────┐
│ 步骤 4：写入数据库                                 │
│   documents 表存文档元信息（标题、类型）            │
│   document_chunks 表存每个 chunk 的文本+向量       │
└──────────────────────────────────────────────────┘
```

#### 代码详解

**`app/services/document_service.py`** — 文档处理的"总指挥"

```python
async def upload_and_index(self, user_id, filename, file_content):
    # 步骤 1：解析文件
    # parse_upload_file 会根据文件后缀名选择解析方式
    # 返回值：(文件类型, 纯文本内容)
    source_type, text = parse_upload_file(filename, file_content)

    # 步骤 2：切分文本
    # 把一整篇文章切成多个 500 字的小块
    # 比如一篇 2000 字的文章会被切成约 4-5 个 chunk
    chunks_text = split_text(text)

    # 步骤 3：批量向量化
    # 把所有 chunk 一次性发给 embedding 模型，返回每个 chunk 的向量
    embeddings = await self.embedding_service.embed_texts(chunks_text)

    # 步骤 4：写入数据库
    # 先创建文档记录
    document = Document(title=filename, source_type=source_type, user_id=user_id)
    self.db.add(document)
    await self.db.flush()  # flush 让数据库分配 document.id，但还没最终提交

    # 再为每个 chunk 创建一条记录
    for idx, (chunk_content, embedding) in enumerate(zip(chunks_text, embeddings)):
        chunk = DocumentChunk(
            document_id=document.id,  # 关联到上面的文档
            chunk_index=idx,          # 这是第几个片段
            content=chunk_content,    # 片段的文本内容
            embedding=json.dumps(embedding),  # 向量转成 JSON 字符串存储
        )
        self.db.add(chunk)

    await self.db.commit()  # 最终提交，所有数据一起写入数据库
    return document, len(chunks_text)
```

**为什么要切分成小块？**
- LLM 的上下文窗口有限，不能把整本书一次性塞进去
- 切成小块后，可以只检索最相关的几个片段，既省 token 又提高准确率
- 重叠 50 字是为了避免在句子中间切断，保证语义完整性

**`app/utils/file_parser.py`** — 文件解析器

```python
def parse_upload_file(filename: str, content: bytes) -> tuple[str, str]:
    # 根据文件后缀名判断类型
    if filename.endswith(".txt"):
        # txt 文件：先把字节流解码成字符串
        # 先尝试 UTF-8（最常见的编码），失败了用 GBK（中文 Windows 常用）
        text = content.decode("utf-8")
        return "txt", text

    if filename.endswith(".pdf"):
        # pdf 文件：用 pypdf 库逐页提取文字
        reader = PdfReader(BytesIO(content))
        pages = []
        for page in reader.pages:
            pages.append(page.extract_text())
        return "pdf", "\n".join(pages)
```

**`app/utils/text_splitter.py`** — 文本切分器

```python
def split_text(text: str) -> list[str]:
    splitter = RecursiveCharacterTextSplitter(
        chunk_size=500,      # 每个块最多 500 个字符
        chunk_overlap=50,    # 相邻块之间重叠 50 个字符
    )
    return splitter.split_text(text)
    # 返回值示例：["第1块文本...", "第2块文本...", "第3块文本..."]
```

**`app/services/embedding_service.py`** — 向量化服务

```python
class EmbeddingService:
    def __init__(self):
        # 加载本地的中文语义模型（第一次运行会自动下载，约 400MB）
        # 这个模型能把中文文本转成 768 维的向量
        self._model = SentenceTransformer("shibing624/text2vec-base-chinese")

    async def embed_texts(self, texts: list[str]) -> list[list[float]]:
        # 批量把文本列表转成向量列表
        # normalize_embeddings=True 表示归一化，方便后面计算余弦相似度
        embeddings = self._model.encode(texts, normalize_embeddings=True)
        return [vec.tolist() for vec in embeddings]
        # 返回值示例：[[0.1, -0.3, 0.5, ...], [0.2, 0.1, -0.4, ...], ...]
        #              ↑ 第1个chunk的768维向量    ↑ 第2个chunk的768维向量
```

**什么是 Embedding（向量化）？**
把文字变成一组数字（向量），语义相近的文字会被转成相近的向量。比如"猫"和"狗"的向量会比较接近，而"猫"和"汽车"的向量会离得很远。这样就可以用数学方法（余弦相似度）来衡量两段文字是否相关。

---

### 4.3 RAG 检索问答流程（重点）

这是另一个核心流程。当用户提出问题时，系统要先找到最相关的文档片段，再让 LLM 基于这些片段生成回答。

#### 流程图

```
用户输入问题："这篇论文的主要结论是什么？"
    │
    ▼
┌──────────────────────────────────────────────────┐
│ 步骤 1：问题向量化                                 │
│   把用户的问题也用 text2vec 模型转成 768 维向量     │
│   app/services/embedding_service.py → embed_query │
└──────────────┬───────────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────────┐
│ 步骤 2：向量检索                                   │
│   从数据库取出当前用户所有文档 chunk 的向量         │
│   计算问题向量与每个 chunk 向量的余弦相似度         │
│   按相似度降序排序，取 Top-5 最相关的片段           │
│   app/services/rag_service.py → _retrieve_chunks  │
└──────────────┬───────────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────────┐
│ 步骤 3：拼接上下文                                 │
│   把 Top-5 片段的文本拼成一段上下文                 │
│   app/services/rag_service.py → _build_context    │
└──────────────┬───────────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────────┐
│ 步骤 4：调用 LLM 生成回答                          │
│   把上下文 + 问题组装成 Prompt                      │
│   发给小米 MiMo API，流式返回回答                   │
│   app/services/rag_service.py → stream_answer     │
└──────────────────────────────────────────────────┘
```

#### 代码详解

**`app/services/rag_service.py`** — RAG 核心服务

```python
# 步骤 2：向量检索
async def _retrieve_chunks(self, query: str, user_id: int = None):
    # 先把用户的问题转成向量
    query_vector = await self.embedding_service.embed_query(query)

    # 从数据库取出这个用户所有文档的 chunk
    stmt = select(DocumentChunk).where(DocumentChunk.tenant_id == self.tenant_id)
    result = await db.execute(stmt)
    all_chunks = list(result.scalars().all())

    # 计算问题向量和每个 chunk 向量的余弦相似度
    chunks_with_score = []
    for chunk in all_chunks:
        embedding = json.loads(chunk.embedding)  # 从 JSON 字符串还原成列表
        similarity = cosine_similarity(query_vector, embedding)
        chunks_with_score.append((chunk, similarity))

    # 按相似度从高到低排序，取前 5 个
    chunks_with_score.sort(key=lambda x: x[1], reverse=True)
    return [chunk for chunk, _ in chunks_with_score[:5]]


# 余弦相似度计算（衡量两个向量有多"像"）
def cosine_similarity(vec1, vec2):
    # 公式：cos(θ) = (A·B) / (|A| × |B|)
    # 结果范围：-1 到 1，越接近 1 表示越相似
    dot_product = sum(a * b for a, b in zip(vec1, vec2))  # 点积
    norm1 = sum(a * a for a in vec1) ** 0.5  # 向量 A 的长度
    norm2 = sum(b * b for b in vec2) ** 0.5  # 向量 B 的长度
    return dot_product / (norm1 * norm2)


# 步骤 3：拼接上下文
def _build_context(self, chunks):
    # 把多个 chunk 拼成一段文字，加上编号方便 LLM 引用
    parts = []
    for i, chunk in enumerate(chunks, start=1):
        parts.append(f"[片段{i}]\n{chunk.content}")
    return "\n\n".join(parts)


# 步骤 4：构建 LLM 调用链
def _build_chain(self):
    # Prompt 模板：告诉 LLM 它的角色和任务
    prompt = ChatPromptTemplate.from_messages([
        ("system", "你是一个基于给定文档片段回答问题的助手。"
                   "请仅根据提供的上下文作答；若上下文中没有相关信息，"
                   "请明确说明无法从文档中找到答案。"),
        ("human", "上下文：\n{context}\n\n问题：{question}\n\n请回答："),
    ])

    # 创建 LLM 客户端（连接小米 MiMo API）
    llm = ChatOpenAI(
        model="mimo-v2.5-pro",           # 使用的模型
        api_key="你的API Key",             # 认证密钥
        base_url="https://token-plan-cn.xiaomimimo.com/v1",  # API 地址
        streaming=True,                    # 启用流式输出（逐字返回）
    )

    # 用 LangChain 的管道语法组装链：Prompt → LLM → 输出解析
    return prompt | llm | StrOutputParser()


# 完整的流式问答
async def stream_answer(self, question, user_id=None):
    chunks = await self._retrieve_chunks(question, user_id=user_id)  # 检索
    context = self._build_context(chunks)                             # 拼上下文
    chain = self._build_chain()                                       # 构建链

    # 流式调用 LLM，逐个 token 返回
    async for token in chain.astream({"context": context, "question": question}):
        yield token  # yield 把每个 token 传给前端，实现"打字机"效果
```

**什么是余弦相似度？**
想象两个箭头，余弦相似度衡量的是它们指向的方向有多一致。值为 1 表示方向完全相同（语义完全一致），0 表示垂直（不相关），-1 表示方向相反。在 RAG 中，我们找的就是和问题方向最接近的那几个文档片段。

**什么是流式输出（Streaming）？**
LLM 生成回答是一个字一个字"蹦"出来的，流式输出就是每生成一个字就立刻发给前端，而不是等全部生成完再一次性返回。用户体验上就是"打字机"效果，不用干等。

---

## 5. 数据库设计

### 5.1 ER 关系图

```
┌───────────┐       ┌──────────────┐       ┌─────────────────┐
│   users   │       │  documents   │       │ document_chunks │
├───────────┤       ├──────────────┤       ├─────────────────┤
│ id (PK)   │◄──┐   │ id (PK)      │◄──┐   │ id (PK)         │
│ username  │   └───│ user_id (FK) │   └───│ document_id (FK)│
│ password  │       │ title        │       │ chunk_index     │
│ _hash     │       │ source_type  │       │ content         │
│ tenant_id │       │ tenant_id    │       │ embedding       │
│ created_at│       │ created_at   │       │ tenant_id       │
│ updated_at│       │ updated_at   │       │ created_at      │
└───────────┘       └──────────────┘       │ updated_at      │
                                           └─────────────────┘
一个用户有多个文档    一个文档有多个 chunk
 (一对多)              (一对多)
```

### 5.2 表字段说明

**users 表（用户表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer, 主键 | 用户唯一 ID，自动递增 |
| username | String(128) | 用户名，同一租户内唯一 |
| password_hash | String(256) | bcrypt 加密后的密码 |
| email | String(256), 可空 | 邮箱（预留字段，当前未使用） |
| tenant_id | String(64) | 租户 ID，默认为 "default" |
| created_at | DateTime | 注册时间 |
| updated_at | DateTime | 最后更新时间 |

**documents 表（文档表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer, 主键 | 文档唯一 ID |
| user_id | Integer, 外键 | 上传者 ID，关联 users.id |
| title | String(512) | 文档标题（默认用文件名） |
| source_type | String(32) | 文件类型："txt" 或 "pdf" |
| original_filename | String(512) | 原始文件名 |
| tenant_id | String(64) | 租户 ID |
| created_at | DateTime | 上传时间 |

**document_chunks 表（文档切片表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer, 主键 | 切片唯一 ID |
| document_id | Integer, 外键 | 所属文档 ID，关联 documents.id |
| chunk_index | Integer | 切片在文档中的顺序（从 0 开始） |
| content | Text | 切片的文本内容 |
| embedding | Text | 向量，JSON 格式的 768 维浮点数组 |
| tenant_id | String(64) | 租户 ID |
| created_at | DateTime | 创建时间 |

### 5.3 数据库迁移

用 **Alembic** 管理数据库表结构的变更：

- `001_initial_schema.py`：创建三张基础表（users、documents、document_chunks）
- `002_add_password_hash.py`：给 users 表新增 password_hash 字段

运行 `alembic upgrade head` 就会自动执行所有未执行的迁移，保证数据库表结构和代码一致。

---

## 6. 关键模块详解

### 6.1 core/ — 核心配置层

**`config.py`** — 配置管理

```python
class Settings(BaseSettings):
    # 这个类定义了所有配置项
    # pydantic-settings 会自动从 .env 文件或环境变量读取值
    database_url: str          # PostgreSQL 连接串
    openai_api_key: str        # LLM API 密钥
    openai_base_url: str       # LLM API 地址
    openai_llm_model: str      # 使用的 LLM 模型名
    chunk_size: int = 500      # 文本切分的块大小
    chunk_overlap: int = 50    # 相邻块的重叠字符数
    retrieval_top_k: int = 5   # 检索时返回最相关的 5 个片段
    embedding_dimension: int = 768  # 向量维度

@lru_cache  # 缓存装饰器：只创建一次 Settings 实例，避免重复读取 .env
def get_settings() -> Settings:
    return Settings()
```

**`database.py`** — 数据库连接管理

```python
# 创建异步引擎（连接池）
engine = create_async_engine(settings.database_url, pool_pre_ping=True)
# pool_pre_ping=True：每次从池中取连接前先"ping"一下，确保连接没断

# 会话工厂：每个请求创建一个独立的数据库会话
AsyncSessionLocal = async_sessionmaker(bind=engine, class_=AsyncSession)

async def get_db():
    # FastAPI 依赖注入：每个请求自动获得一个数据库会话
    # 请求结束后自动关闭会话
    async with AsyncSessionLocal() as session:
        yield session
```

**`deps.py`** — 公共依赖

```python
async def get_tenant_id(x_tenant_id: str = Header(None)) -> str:
    # 从请求头 X-Tenant-ID 解析租户 ID
    # 如果没传，返回默认值 "default"
    if x_tenant_id and x_tenant_id.strip():
        return x_tenant_id.strip()
    return "default"
```

### 6.2 models/ — 数据库模型层

使用 SQLAlchemy 的声明式模型，每个类对应数据库中的一张表。

**`base.py`** — 基类和 Mixin

```python
class Base(DeclarativeBase):
    """所有 ORM 模型的基类"""
    pass

class TimestampMixin:
    """给表自动添加 created_at 和 updated_at 字段"""
    created_at: Mapped[datetime] = mapped_column(server_default=func.now())
    updated_at: Mapped[datetime] = mapped_column(onupdate=func.now())

class TenantMixin:
    """给表添加 tenant_id 字段，实现多租户数据隔离"""
    tenant_id: Mapped[str] = mapped_column(String(64), index=True)
```

**`user.py`** — 用户模型

```python
class User(Base, TenantMixin, TimestampMixin):
    __tablename__ = "users"
    id: Mapped[int] = mapped_column(primary_key=True)
    username: Mapped[str] = mapped_column(String(128))
    password_hash: Mapped[str] = mapped_column(String(256))
    # relationship：一个用户有多个文档（一对多关系）
    documents: Mapped[list["Document"]] = relationship(cascade="all, delete-orphan")
    # cascade="all, delete-orphan"：删除用户时，自动删除他的所有文档
```

### 6.3 schemas/ — 请求/响应模型

用 Pydantic 定义 API 的输入输出格式，FastAPI 会自动校验数据。

```python
class ChatRequest(BaseModel):
    question: str = Field(..., min_length=1)  # 必填，不能为空
    user_id: int | None = None                 # 可选

class RegisterRequest(BaseModel):
    username: str = Field(..., min_length=2, max_length=128)
    password: str = Field(..., min_length=6, max_length=128)
```

### 6.4 services/ — 业务逻辑层

这是系统的核心，包含三个服务：

- **`document_service.py`**：文档上传的完整流水线（解析→切分→向量化→入库）
- **`embedding_service.py`**：封装本地 text2vec 模型，提供文本转向量的能力
- **`rag_service.py`**：RAG 检索生成的完整流程（向量检索→拼上下文→调 LLM）

### 6.5 api/ — API 路由层

每个文件对应一组相关的接口：

- **`users.py`**：`POST /register`（注册）、`POST /login`（登录）
- **`documents.py`**：`POST /upload`（上传文档）、`GET /list`（文档列表）
- **`chat.py`**：`POST /retrieve`（仅检索）、`POST /stream`（流式问答）

### 6.6 utils/ — 工具函数

- **`file_parser.py`**：根据文件后缀名选择解析方式（txt 解码 / pdf 提取）
- **`text_splitter.py`**：封装 LangChain 的 RecursiveCharacterTextSplitter

---

## 7. 接口清单

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/users/register` | 用户注册（用户名 + 密码） |
| POST | `/api/v1/users/login` | 用户登录（验证密码） |
| POST | `/api/v1/documents/upload` | 上传文档（multipart/form-data） |
| GET | `/api/v1/documents/list?user_id=X` | 获取用户文档列表 |
| POST | `/api/v1/chat/retrieve` | 仅检索 Top-K 相关片段（不调 LLM） |
| POST | `/api/v1/chat/stream` | RAG 流式问答（SSE 格式） |
| GET | `/health` | 健康检查 |
| GET | `/` | 前端页面 |

---

## 8. 配置说明

`.env` 文件中的配置项：

```env
# PostgreSQL 数据库连接
DATABASE_URL=postgresql+asyncpg://postgres:123456@localhost:5433/rag_db

# 小米 MiMo LLM API
OPENAI_API_KEY=你的API密钥
OPENAI_BASE_URL=https://token-plan-cn.xiaomimimo.com/v1
OPENAI_LLM_MODEL=mimo-v2.5-pro

# Embedding 模型（本地，无需 API）
# 使用 shibing624/text2vec-base-chinese，768 维

# 文本切分参数
CHUNK_SIZE=500       # 每个块最多 500 字符
CHUNK_OVERLAP=50     # 相邻块重叠 50 字符

# 向量检索
RETRIEVAL_TOP_K=5    # 返回最相关的 5 个片段

# 向量维度
EMBEDDING_DIMENSION=768
```

---

## 9. 启动与部署

### 9.1 环境要求

- Python 3.10+
- PostgreSQL 16（端口 5433）
- 网络能访问小米 MiMo API

### 9.2 启动步骤

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 启动 PostgreSQL（确保端口 5433，密码 123456）

# 3. 创建数据库
createdb -U postgres -p 5433 rag_db

# 4. 执行数据库迁移
alembic upgrade head

# 5. 启动服务器
python start_server.py
# 或者直接用 uvicorn：
# uvicorn app.main:app --host 0.0.0.0 --port 8080

# 6. 访问 http://localhost:8080
```

### 9.3 start_server.py 做了什么

```python
# 清除系统环境变量，防止它们覆盖 .env 中的配置
for key in ['OPENAI_API_KEY', 'OPENAI_BASE_URL', ...]:
    os.environ.pop(key, None)

# 启动 uvicorn 服务器，开启热重载（代码修改后自动重启）
uvicorn.run("app.main:app", host="0.0.0.0", port=8080, reload=True)
```

---

## 10. 面试扩展点 / 可优化方向

### 10.1 向量检索优化

**当前方案**：把所有 chunk 的向量从数据库取出来，在 Python 中逐一计算余弦相似度。

**问题**：数据量大时（比如 10 万个 chunk），每次查询都要全量遍历，速度很慢。

**优化方案**：
- **pgvector**：PostgreSQL 的向量检索扩展，支持在数据库内做向量相似度查询，支持 HNSW/IVFFlat 索引
- **Milvus / Qdrant**：专业向量数据库，支持亿级向量的毫秒级检索
- **FAISS**：Facebook 开源的向量检索库，适合单机场景

### 10.2 LLM 可替换性

当前用的是小米 MiMo API，但代码中用的是 LangChain 的 `ChatOpenAI`，只要改 `.env` 里的 API 地址和模型名，就能切换到任何兼容 OpenAI 接口的模型（如 DeepSeek、智谱、月之暗面等）。

### 10.3 多轮对话

当前系统是"一问一答"，没有上下文记忆。可以扩展：
- 维护对话历史（chat history）
- 把历史对话也加入 Prompt，让 LLM 知道之前聊了什么

### 10.4 文档管理

当前只支持上传，不支持删除。可以扩展：
- 文档删除（级联删除 chunk）
- 文档预览
- 批量上传

### 10.5 权限与安全

- 当前密码用 bcrypt 加密，但没有 token/Session 机制（无状态）
- 可以加 JWT 实现登录态管理
- 可以加 API 限流防止滥用

### 10.6 生产部署

- 用 Docker 容器化部署
- Nginx 反向代理 + HTTPS
- 数据库连接池调优
- 日志收集与监控（ELK / Prometheus）
