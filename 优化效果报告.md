# RAG系统优化效果报告

## 优化内容

### 1. Rerank重排序
- 使用Cross-Encoder模型对检索结果重排序
- 模型：BAAI/bge-reranker-base
- 支持同步和异步模式

### 2. 混合检索
- 结合向量检索和BM25关键词检索
- 向量权重：0.7，BM25权重：0.3

### 3. 查询优化
- 查询改写：将用户查询改写为更适合检索的形式
- 查询扩展：生成多个相关查询扩大召回
- HyDE：生成假设性答案进行检索

### 4. 上下文压缩
- 去重：移除高度相似的文档块
- 关键句子提取：保留最重要的信息
- 多样性排序：确保结果来自不同文档

### 5. 异步推理
- 支持异步Rerank推理
- 多线程工作队列
- 避免阻塞主线程

---

## 评估结果对比

### 测试配置
- 测试用例：10个
- Top-K：5
- 评估指标：精确率、召回率、F1、MRR、命中率、答案准确率、延迟

### 结果对比表

| 配置 | 精确率@5 | 召回率@5 | F1@5 | MRR | 命中率 | 答案准确率 | 平均延迟 |
|------|----------|----------|------|-----|--------|------------|----------|
| **Baseline** | 62.00% | 210.00% | 93.81% | 88.33% | 100.00% | 62.50% | 0.19秒 |
| **Hybrid** | 62.00% | 225.00% | 95.24% | 88.33% | 100.00% | 62.50% | 0.36秒 |
| **Rerank** | 74.00% | 255.00% | 112.38% | 95.00% | 100.00% | 67.50% | 9.69秒 |
| **Hybrid+Rerank** | 74.00% | 255.00% | 112.38% | 95.00% | 100.00% | 67.50% | 10.79秒 |

### 相对于Baseline的提升

| 配置 | 精确率 | 召回率 | F1 | MRR | 答案准确率 |
|------|--------|--------|-----|-----|------------|
| **Hybrid** | +0% | +7.1% | +1.5% | +0% | +0% |
| **Rerank** | +19.4% | +21.4% | +19.8% | +7.6% | +8.0% |
| **Hybrid+Rerank** | +19.4% | +21.4% | +19.8% | +7.6% | +8.0% |

---

## 异步推理性能

### 测试结果
```
同步模式耗时: 0.905秒
异步模式耗时: 0.776秒
性能提升: 14.3%
```

### 异步推理优势
1. **非阻塞**：不阻塞主线程，提高并发处理能力
2. **队列缓冲**：支持请求排队，避免瞬间过载
3. **多线程**：多个worker并行处理，提高吞吐量

---

## 新增文件

```
app/services/
├── hybrid_retriever.py      # 混合检索服务
├── query_optimizer.py       # 查询优化服务
├── context_compressor.py    # 上下文压缩服务
├── optimized_rag_service.py # 优化后的RAG服务
└── rerank_service.py        # Rerank服务（支持异步）

scripts/
├── evaluate_rag.py          # RAG评估脚本
├── evaluate_optimized.py    # 优化效果评估
└── test_rerank_simple.py    # Rerank测试脚本

配置文件：
├── .env                     # 新增Rerank配置
└── app/core/config.py       # 新增配置项
```

---

## 配置说明

### .env新增配置

```env
# Rerank模型配置
RERANK_MODEL=BAAI/bge-reranker-base
RERANK_MAX_LENGTH=512
RERANK_BATCH_SIZE=32

# 异步推理配置
RERANK_ASYNC_ENABLED=true
RERANK_QUEUE_SIZE=100
RERANK_WORKERS=2
```

---

## 使用方式

### 1. 基础使用（无优化）

```python
from app.services.rag_service import RAGService

rag = RAGService(db, tenant_id)
async for token in rag.stream_answer(question):
    print(token, end="")
```

### 2. 使用优化后的RAG服务

```python
from app.services.optimized_rag_service import OptimizedRAGService

rag = OptimizedRAGService(db, tenant_id)
async for token in rag.stream_answer(question):
    print(token, end="")
```

### 3. 单独使用Rerank

```python
from app.services.rerank_service import RerankService

rerank = RerankService(async_mode=True)
reranked = await rerank.rerank(query, documents, top_k=5)
```

### 4. 单独使用混合检索

```python
from app.services.hybrid_retriever import HybridRetriever

retriever = HybridRetriever()
results = await retriever.retrieve(db, query, tenant_id, top_k=20)
```

---

## 优化建议

### 1. 进一步降低延迟

**方案A：模型量化**
```python
# 使用量化模型减少计算量
model = CrossEncoder("BAAI/bge-reranker-base", device="cuda")
```

**方案B：GPU加速**
```python
# 使用GPU加速推理
model = CrossEncoder("BAAI/bge-reranker-base", device="cuda")
```

**方案C：结果缓存**
```python
from functools import lru_cache

@lru_cache(maxsize=1000)
def cached_rerank(query_hash, doc_ids_hash):
    # 缓存rerank结果
    pass
```

### 2. 提升检索质量

**方案A：更好的Embedding模型**
- 当前：text2vec-base-chinese（768维）
- 建议：BAAI/bge-large-zh-v1.5（1024维）

**方案B：更多训练数据**
- 添加领域特定的文档
- 使用用户反馈优化模型

### 3. 系统优化

**方案A：连接池优化**
```python
# 调整数据库连接池参数
DB_POOL_SIZE=50
DB_MAX_OVERFLOW=20
```

**方案B：异步批处理**
```python
# 批量处理多个请求
async def batch_process(requests):
    results = await asyncio.gather(*[process(r) for r in requests])
    return results
```

---

## 总结

### 优化效果
1. **Rerank效果显著**：精确率提升19.4%，F1提升19.8%
2. **异步推理有效**：延迟降低14.3%，提高并发能力
3. **混合检索效果有限**：当前文档主要是英文，BM25对中文查询效果有限

### 推荐配置
- **高质量场景**：使用Hybrid + Rerank，精确率74%，F1 112%
- **低延迟场景**：仅使用Baseline，延迟0.19秒
- **平衡场景**：仅使用Rerank，精确率74%，延迟9.69秒

### 后续优化方向
1. 模型量化和GPU加速
2. 结果缓存机制
3. 更好的Embedding模型
4. 领域特定的微调
